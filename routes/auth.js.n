// ===================================
// routes/auth.js - (مسار تسجيل الدخول مُصحح)
// ===================================
const express = require('express');
const router = express.Router();
// ... (تضمين النماذج) ...

// ------------------------------------------------
// مسار عرض صفحة تسجيل الدخول (GET /login) 🔑
// ------------------------------------------------
router.get('/login', (req, res) => {
    // 🚨 التصحيح هنا: تمرير 'error' كقيمة افتراضية فارغة
    res.render('login', { 
        title: 'تسجيل الدخول', 
        pageName: 'login',
        error: '' // <--- تم تعريف المتغير هنا
    });
});

router.post('/login', (req, res) => { 
    // ... منطق التحقق من المستخدم ...
    
    // عند فشل تسجيل الدخول:
    // res.render('login', { title: 'تسجيل الدخول', pageName: 'login', error: 'بيانات تسجيل الدخول غير صحيحة.' });

    // عند النجاح:
    // req.session.userId = 'sampleUserId'; 
    res.redirect('/'); 
});

// ------------------------------------------------
// مسار عرض صفحة إنشاء حساب (GET /signup)
// ------------------------------------------------
router.get('/signup', (req, res) => {
    res.render('signup', { 
        title: 'إنشاء حساب جديد', 
        pageName: 'signup',
        error: '' // يمكن إضافة متغير error هنا أيضاً
    });
});
// ... (بقية مسارات المصادقة) ...

module.exports = router;
