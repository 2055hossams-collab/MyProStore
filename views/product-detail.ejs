<%# =================================== %>
<%# views/product-detail.ejs - مصحح %>
<%# =================================== %>
<%- include('partials/header', {title: title, pageName: pageName}) %>

<div class="container my-5">
    
    <div class="row">
        <div class="col-md-6 mb-4">
             <%# ... كود عرض الصورة والتفاصيل الأساسية هنا ... %>
        </div>
        <div class="col-md-6">
            <h1 class="display-5 fw-bold mb-3"><%= product.name %></h1>
            <%# ... كود العرض والسعر وأزرار العربة هنا ... %>
        </div>
    </div>
    
    <hr class="my-5">

    <section class="similar-products">
        <h3 class="mb-4 text-center">قد يعجبك أيضاً</h3>
        
        <div class="row">
            <%# 🚨 التصحيح هنا: استخدام المتغير products الممرر من المسار %>
            <% if (products && products.length > 0) { %>
                <% products.forEach(p => { %>
                    <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                        <div class="card h-100 shadow-sm">
                            <img src="<%= p.img || '/img/default-product.png' %>" class="card-img-top" alt="<%= p.name %>" style="height: 150px; object-fit: cover;">
                            <div class="card-body">
                                <h5 class="card-title text-truncate"><%= p.name %></h5>
                                <p class="fw-bold text-primary"><%= p.price %> ريال</p>
                                <a href="/product/<%= p._id %>" class="btn btn-sm btn-outline-info">عرض</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12 text-center text-muted">لا توجد منتجات مشابهة متاحة حالياً.</div>
            <% } %>
        </div>
    </section>
    
</div>

<%- include('partials/footer') %>
